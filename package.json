{
  "name": "laromanita",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "cf:login": "wrangler login",
    "cf:kv:create": "wrangler kv namespace create SESSION",
    "cf:kv:create:preview": "wrangler kv namespace create SESSION --preview",
    "cf:deploy:dry": "npm run build && wrangler deploy --dry-run",
    "cf:deploy": "npm run build && wrangler deploy",
    "test": "vitest run",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.1",
    "@supabase/supabase-js": "^2.105.4",
    "astro": "^6.4.2",
    "resend": "^3.0.0",
    "wrangler": "^4.90.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@testing-library/jest-dom": "^6.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "overrides": {
    "vite": "^7"
  }
}
